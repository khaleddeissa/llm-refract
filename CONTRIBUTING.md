# Contributing

Run `make setup`, `make hooks`, and `make ci` before opening a PR.
Use Conventional Commits. Changes to wire contracts need cross-language fixtures.
Document unsupported behavior explicitly and keep providers out of the canonical model.
See [ARCHITECTURE.md](ARCHITECTURE.md) for module boundaries.
