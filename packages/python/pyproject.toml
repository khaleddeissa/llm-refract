[project]
name = "refract"
version = "0.1.0"
description = "Provider-agnostic recording for portable AI executions"
requires-python = ">=3.11"
license = "Apache-2.0"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
